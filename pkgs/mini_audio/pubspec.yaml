name: mini_audio
description: An example for `package:code_assets` with miniaudio.
version: 0.1.0
repository: https://github.com/dart-lang/native/tree/main/pkgs/code_assets/example/build/mini_audio

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  code_assets: ^1.0.0
  ffi: ^2.1.4
  hooks: ^1.0.3
  logging: ^1.3.0
  meta: ^1.11.0
  native_toolchain_c: ^0.18.0
  record_use: ^0.6.0

dev_dependencies:
  ffigen: 21.0.0-dev.0
  test: ^1.25.15
